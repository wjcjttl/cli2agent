[project]
name = "cli2agent-executor-skill"
version = "0.1.0"
description = "OpenClaw skill for delegating tasks to Claude Code via cli2agent"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
]
